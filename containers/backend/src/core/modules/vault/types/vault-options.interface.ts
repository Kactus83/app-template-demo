/**
 * Interface décrivant les options nécessaires pour se connecter à Vault.
 * @interface IVaultOptions
 * @category Core
 */
export interface IVaultOptions {
  vaultAddress: string;
  credentialsPath: string;
}
